# Java Object-Oriented Programming (OOP) ☕

A structured repository containing my Java coursework and lab assignments. This collection demonstrates my progression in building robust backend logic, understanding system architecture, and applying core object-oriented principles.

### 📂 Repository Structure
* **`/OOP-JAVA`**: Contains all lab implementations focusing on classes, inheritance, polymorphism, encapsulation, and problem-solving logic.

### 🚀 How to Run
All files in this repository are standard Java source files (`.java`). They can be compiled and executed using the standard Java Development Kit (JDK) via the command line:

```bash
javac filename.java
java filename
